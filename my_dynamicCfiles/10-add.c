#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @fNum: first number input
 * @secNum: second number input
 * Return: 1 + 2
 */
int add(int fNum, int secNum)
{
	return (fNum + secNum);
}
