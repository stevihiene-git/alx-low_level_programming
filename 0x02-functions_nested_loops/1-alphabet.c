#include "_putchar.h"
#include <stdio.h>

void print_alphabet(void)
{
	 char ch;

        for (ch = 'a' ; ch <= 'z' ; ch++)
            _putchar(ch);
{

#include "_putchar.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    print_alphabet();
    _putchar();
    return (0);
}
